# ── Dependencies ──────────────────────────────────────────────────────────
node_modules
.pnp
.pnp.js

# ── Build outputs ─────────────────────────────────────────────────────────
dist
dist-ssr
build
*.local

# ── Testing & coverage ────────────────────────────────────────────────────
coverage
playwright-report
test-results
*.lcov
.nyc_output

# ── Editor & OS ───────────────────────────────────────────────────────────
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ── Logs ──────────────────────────────────────────────────────────────────
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── Environment & secrets ─────────────────────────────────────────────────
.env
.env.local
.env.*.local
.env.development.local
.env.production.local
*.pem

# ── Vercel ────────────────────────────────────────────────────────────────
.vercel

# ── Cache ─────────────────────────────────────────────────────────────────
.cache
.turbo
.parcel-cache
.eslintcache
.stylelintcache
*.tsbuildinfo

# ── Misc workspace artifacts ──────────────────────────────────────────────
audits/screenshots
.claude/skill-runs
playground/dist

# ── Temporary BC4 / Recraft outputs (mohou se regenerovat) ────────────────
public/recraft-output/_staging

# ── Kvalt DS heavy binární assets (regenerovatelné, do gitu nepatří) ──────
# Pokud Kvalt DS docs site potřebuje tyto assety v produkci, řešit přes
# CDN (R2 / S3) nebo Git LFS, ne přímo v repu.
# public/illustrations — ignorujeme PNG/GIF/WEBP, ale _catalog.json a optimized/
# adresář ponecháme tracked (Kvalt SidebarSearch je build-time importuje)
public/illustrations/*.png
public/illustrations/*.gif
public/illustrations/*.webp
public/illustrations/*.jpg
public/illustrations/*.jpeg
public/illustrations/optimized
public/recraft-output
knowledge/design-inspiration
knowledge/recraft-styles
pencil/assets
pencil/images
pencil/*.pen

# ── Velké standalone assety v public root ─────────────────────────────────
public/code-panel-gradient.png

# ── BC4 video assety (upload přes Vercel CLI nebo CDN, ne přes git) ───────
public/videos/*.mp4
public/videos/*.webm
public/videos/*.mov
